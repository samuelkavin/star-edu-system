import {StudentStatusValidationPipe} from './student-status-validation.pipe';

describe('StudentStatusValidationPipe', () => {
  it('should be defined', () => {
    expect(new StudentStatusValidationPipe()).toBeDefined();
  });
});
